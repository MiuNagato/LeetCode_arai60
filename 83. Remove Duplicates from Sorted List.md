# 進め方

Step1 : 3回続けてエラーが出ないように書く。

Step2 : 他の人のPRを参照し、コメントする。ドキュメントを参照する。

Step3 : 問題を解く。

# 実践

## Step1

### 3回連続で再現

141の今度はreturnをvisitedに変更だけだと思ったので、ほぼコピー
```python
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        visited = set()
        node = head
        while(node is not None):
            if(!node in visited)
                visited.add(node)
            node = node.next
        return visited
```

pythonの否定は!では書けないらしい。正しくは
```python
            if(node not in visited): コロンも忘れない
```

オブジェクトそのまま渡すと重複判定できないのでnode.valueにする
```python
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        visited = set()
        node = head
        while(node is not None):
            if(node.val not in visited):
                visited.add(node.val)
            node = node.next
        return visited
```
😨
```
TypeError: {1, 2} is not valid value for the expected return type ListNode
    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    raise TypeError(str(ret) + " is not valid value for the expected return type ListNode");
Line 45 in _driver (Solution.py)
    ~~~~~~~^^
    _driver()
Line 54 in <module> (Solution.py)
During handling of the above exception, another exception occurred:
Exception: Type <class 'set'> cannot be serialized
Line 138 in serializer_node (./python3/__serializer__.py)
Line 181 in _serialize_default (./python3/__serializer__.py)
Line 200 in _serialize (./python3/__serializer__.py)
    out = ser._serialize(ret, 'ListNode')
Line 43 in _driver (Solution.py)
```

解答例
```python
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node = head

        while node and node.next:
            if node.val == node.next.val:
                node.next = node.next.next
            else:
                node = node.next

        return head
```

## Step2

### 同じ問題を解いた人のプルリクを見る

```python
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]: # 右側が戻り値の型
        node = head
        while node and node.next:
            if node.val == node.next.val:
                node.next = node.next.next
            else:
                node = node.next

        return head # まだ先頭ノードを指してるheadを返す
```

- IfとContinueで書く

```python
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node = head
        while node:
            if node.next and node.next.val == node.val: # node.next はwhileで判定の方が読みやすいかな
                node.next = node.next.next
                continue # whileの先頭に戻る
            node = node.next
        return head
```

- 再帰で書く

```python
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        # 終了条件
        if head is None or head.next is None:
            return head
        if head.val == head.next.val:
            return self.deleteDuplicates(head.next)
        else:
            head.next = self.deleteDuplicates(head.next)
            return head
```

1223だと最初elseに入ってhead.nextに23、最後は123でreturn

## Step3

### 問題を解く

ifに入った場合でも毎回次のノードに進む方が自然に感じたので、elseを外した

```python
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node = head
        while(node and node.next):
            if(node.val == node.next.val):
                node.next = node.next.next
            node = node.next
        return head
```

！３つ以上重複している場合に取り除き損ねる！
のでやっぱり最初の書き方で。

```python
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node = head
        while(node and node.next):
            if(node.val == node.next.val):
                node.next = node.next.next
            else:
                node = node.next
        return head
```
